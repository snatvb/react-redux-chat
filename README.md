# react-redux-chat

This chat use sokets (socket.io).
Warning: You can't send message, when user is offline. Messages will be remove when user leaves the network.

##Get start
Move to server and run <code>npm i && npm start</code>
